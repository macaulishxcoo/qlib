- 所有可学习模型必须继承 `Model` 基类并实现 `fit` 和 `predict` 方法，且模型属性名不得以 '_' 开头以支持序列化。
- 训练器通过字典形式的 `task_config` 接收模型、数据集及重加权器配置，并利用 `init_instance_by_config` 进行动态实例化。
- 风险模型在 `predict` 中统一处理价格转收益率、数据缩放及 NaN 值填充/掩码逻辑，子类仅需实现 `_predict` 核心估计算法。
- 采用配置字典（dict）或字符串路径结合 `init_instance_by_config` 工具函数动态实例化策略、执行器等组件，实现松耦合配置。
- 广泛使用 `yield from` 构建生成器管道（如 `collect_data_loop`），支持在回测过程中流式收集交易决策数据以用于强化学习训练。
- 通过 `CommonInfrastructure` 和 `LevelInfrastructure` 在嵌套执行层级间共享交易账户、交易所实例及日历信息，确保上下文一致性。
- 订单撮合逻辑中严格区分买入/卖出方向，并在 `Exchange` 中统一处理交易费用（按比例或最小值）及价格取整（trade_unit）。
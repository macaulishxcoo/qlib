- 标签维度标准化：在 `fit` 方法中，若标签为二维数组且第二维为1，则使用 `np.squeeze` 转换为一维数组以满足树模型API要求。
- 统一预测接口：`predict` 方法均接受 `segment` 参数（默认为 "test"），并从 `dataset` 中提取 `feature` 列，返回索引对齐的 `pd.Series`。
- 可选重加权支持：`fit` 方法普遍支持 `reweighter` 参数，若提供则通过 `Reweighter.reweight` 计算样本权重并传入底层训练函数。
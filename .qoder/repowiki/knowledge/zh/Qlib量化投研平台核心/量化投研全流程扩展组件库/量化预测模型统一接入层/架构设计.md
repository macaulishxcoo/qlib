- 采用`try-except`条件导入策略，在`__init__.py`中集中处理CatBoost、LightGBM、XGBoost及PyTorch等可选依赖，确保核心库在缺失特定AI框架时仍可正常加载。
- 将不同技术栈（Sklearn/LightGBM/XGBoost vs PyTorch）的模型类聚合至`all_model_classes`元组，向上层暴露统一的模型注册表，解耦模型实现与模型调用逻辑。
- 通过模块级初始化脚本作为边界契约，强制要求所有子模型遵循Qlib的基类规范，从而实现传统机器学习与深度学习模型在训练流水线中的透明替换。
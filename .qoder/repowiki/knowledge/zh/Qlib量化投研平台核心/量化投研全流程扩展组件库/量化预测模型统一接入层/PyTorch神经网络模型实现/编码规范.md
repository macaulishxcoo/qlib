- 所有PyTorch模型类均继承自qlib.model.base.Model基类，并实现fit和predict接口。
- 每个模型文件通常定义两个类：一个对外交互的Model子类（处理数据加载、训练循环）和一个内部nn.Module子类（定义网络结构）。
- 优化器选择统一支持'adam'和'gd'（SGD），并通过字符串参数动态初始化。
- 训练过程中普遍采用梯度裁剪（clip_grad_value_）以防止梯度爆炸。
- 模型输入通常在forward中进行reshape和permute操作，以适配[Batch, Time, Feature]的时序格式。
- 模块采用统一的Model基类继承结构，每个模型文件包含一个主模型类（如LSTM、TransformerModel）和一个内部定义的PyTorch nn.Module网络结构。
- 通过__init__.py集中导入并暴露所有可用模型类，支持可选依赖的容错加载。
- 共享工具函数（如count_parameters）提取至pytorch_utils.py供多处复用。
- 模型训练流程标准化：均实现fit、predict方法，支持早停、GPU加速及模型保存/加载。
- 策略类通常继承自 `BaseStrategy` 或其子类（如 `BaseSignalStrategy`），并重写 `generate_trade_decision` 或 `generate_target_weight_position` 方法。
- 优化器类遵循 `BaseOptimizer` 规范，通过实现 `__call__` 方法接收市场数据并返回优化后的权重数组。
- 在处理交易日历和订单生成时，广泛使用 `trade_calendar.get_step_time` 获取当前及预测时间窗口，以对齐信号与交易执行。
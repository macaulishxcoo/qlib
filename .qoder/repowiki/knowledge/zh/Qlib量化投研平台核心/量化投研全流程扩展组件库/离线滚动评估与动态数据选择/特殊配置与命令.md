- 运行基础滚动评估：`python -m qlib.contrib.rolling base --conf_path <config.yaml> run`
- 运行 DDG-DA 动态评估：`python -m qlib.contrib.rolling ddgda --conf_path <config.yaml> run`
- 注意：由于 MLflow 实验删除限制，重新运行前需手动清理 `mlruns` 目录以避免命名冲突。
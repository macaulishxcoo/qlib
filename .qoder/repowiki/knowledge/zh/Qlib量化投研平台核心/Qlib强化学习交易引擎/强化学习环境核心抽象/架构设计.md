- 采用泛型基类设计：`Simulator`管理状态流转，`Interpreter`（含`StateInterpreter`和`ActionInterpreter`）负责仿真状态与策略观测/动作间的转换，`Reward`封装奖励逻辑。
- 依赖Gym空间规范：通过`gym.Space`定义观测与动作空间，并实现增强版验证逻辑`_gym_space_contains`以确保数据合规性。
- 模块入口清晰：`__init__.py`统一导出核心组件，`seed.py`定义初始状态类型别名，`aux_info.py`提供可扩展的辅助信息收集接口。
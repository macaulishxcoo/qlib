- 状态与指标数据结构化：统一使用`SAOEState`（NamedTuple）和`SAOEMetrics`（TypedDict）承载环境状态，确保仿真器、解释器和奖励函数间的数据契约一致。
- 解释器模式解耦：通过`StateInterpreter`和`ActionInterpreter`抽象层隔离环境内部状态与智能体观测/动作空间，支持灵活切换全历史或当前步观测模式。
- 仿真器接口标准化：所有仿真器均继承自`Simulator`基类并实现`step`、`get_state`和`done`方法，保持与强化学习交互协议的一致性。
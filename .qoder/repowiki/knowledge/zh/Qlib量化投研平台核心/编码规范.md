- 全局配置对象 `C` (QlibConfig) 作为唯一的状态源，所有子模块通过 `from qlib.config import C` 获取运行时参数，禁止硬编码路径或阈值。
- 采用延迟导入（Lazy Import）模式，在 `init()` 或具体功能函数内部导入重型依赖（如 `mlflow`, `torch`），以降低库的初始加载时间并避免循环依赖。
- 统一使用 `get_module_logger` 获取带有模块前缀的日志记录器，并在 `logging_config` 中集中定义过滤规则（如过滤数据缺失警告），确保全平台日志格式与行为一致。